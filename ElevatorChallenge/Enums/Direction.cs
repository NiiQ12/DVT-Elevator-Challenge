namespace ElevatorChallenge.Enums {
    /// <summary>
    /// Direction enum is used to determine whether a request is made to go up or down using elevator(s)
    /// </summary>
    public enum Direction {
        Up,
        Down
    }
}