namespace ElevatorChallenge.Enums {
    /// <summary>
    /// ElevatorState enum is used to determine what an elevator is busy doing
    /// </summary>
    public enum ElevatorState {
        Inactive,
        Idle,
        MovingUp,
        MovingDown
    }
}